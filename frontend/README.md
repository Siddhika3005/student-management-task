# Frontend

## Overview
React dashboard for authentication and task management. It connects to the backend REST API via Axios.

## Setup
1. Install dependencies:
	- npm install
2. Start the dev server:
	- npm run dev

## Environment
Optional API base URL:
```
VITE_API_URL=http://localhost:5000/api/v1
```
